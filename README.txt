this program is Django ( python ) base

use rest_framework for Restapi Django

we have module like this in myapp :




class Student(models.Model):
    Name=models.CharField(max_length=100)
    Family=models.CharField(max_length=100)
    Code=models.CharField(max_length=100)


after that we must serialize the module in serialize.py using :

from rest_framework import serializers

and our view is like ( using api_view decorator ):

@api_view(["GET"])
def student_list(request):
    students=Student.objects.all()
    students_serializer=StudentSerializer(students , many = True)
    return Response(students_serializer.data)


@api_view(["GET"])
def student_details(request , pk):
    student=Student.objects.get(id=pk)
    student_serializer=StudentSerializer(student , many = False)
    return Response(student_serializer.data)


@api_view(["POST"])
def student_save(request):
    student=StudentSerializer(data=request.data)
    if (student.is_valid()) :
        student.save()

    return Response(student.data)


@api_view(["POST"])
def student_update(request , pk):
    instance=Student.objects.get(id=pk)
    student=StudentSerializer( instance=instance , data=request.data)
    if(student.is_valid()):
        student.save()

    return Response(student.data)   



@api_view(["DELETE"])
def student_delete(request , pk):
    instance=Student.objects.get(id=pk)
    instance.delete()

    return Response("student deleted")    

all of this is myapp


and after that 


we read the api generated by testapp


enjoy!








